# PHP CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-php/ for more details
#
version: 2
jobs:
  deploy:
    machine:
      enabled: true
    steps:
      - checkout
      - run:
          name: "test"
          command: "ls -l"
      - run:
          name: "copy files to web server"
          command: scp -r personal-website/src/* $USR@$HOST:$WS_FOLDER/

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - deploy  

